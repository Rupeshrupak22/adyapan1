# ─── Database (TiDB Cloud / MySQL) ───────────────────────────────────
# IMPORTANT: This MUST point to the same database as the Preschool website backend
# and the adyapan_admin backend. All 3 projects share the same TiDB database.
DATABASE_URL="mysql://username:password@gateway01.ap-southeast-1.prod.alicloud.tidbcloud.com:4000/preschool?sslaccept=strict"

# Raw MySQL (used by Flutter app direct DB connection and backend routes)
MYSQL_HOST=gateway01.ap-southeast-1.prod.alicloud.tidbcloud.com
MYSQL_PORT=4000
MYSQL_USER=username
MYSQL_PASSWORD=password
MYSQL_DATABASE=preschool
MYSQL_SSL=true

# ─── JWT ─────────────────────────────────────────────────────────────
# IMPORTANT: JWT_SECRET must be the SAME across all 3 projects (Preschool, adyapanschool, adyapan_admin)
# so that tokens issued by one backend are valid on the others.
JWT_SECRET=your-strong-random-secret-min-32-chars
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# ─── Server ─────────────────────────────────────────────────────────
PORT=4000
NODE_ENV=development

# ─── CORS (comma-separated origins for production) ───────────────────
# Leave empty for dev (allows all). Set for production:
# CORS_ORIGINS=https://your-frontend.vercel.app,https://your-domain.com
CORS_ORIGINS=

# ─── API Base URL (for Flutter mobile app to call this backend) ──────
# Set this to your deployed backend URL when in production
# API_BASE_URL=https://adyapanschool-backend.onrender.com
