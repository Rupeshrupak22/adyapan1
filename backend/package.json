{
  "name": "adyapanschool-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone backend API for the Adyapan School app.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/config/env.js && node --check src/db.js && node --check src/controllers/auth.controller.js && node --check src/routes/auth.routes.js && node --check scripts/check-db-schema.js",
    "db:schema": "node scripts/check-db-schema.js",
    "db:inspect": "node scripts/inspect-db.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0",
    "mysql2": "^3.11.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
